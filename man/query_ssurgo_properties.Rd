% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soilweb_helpers.R
\name{query_ssurgo_properties}
\alias{query_ssurgo_properties}
\title{Query SSURGO tabular properties from SDA}
\usage{
query_ssurgo_properties(
  mukeys,
  properties,
  method = "Weighted Average",
  top_depth = 0,
  bottom_depth = 25
)
}
\arguments{
\item{mukeys}{Integer or character vector of SSURGO map unit keys. Must
not be empty.}

\item{properties}{Character vector of SDA property names to retrieve
(e.g. \code{"claytotal_r"}, \code{"sandtotal_r"}). Passed to the \code{property}
argument of \code{\link[soilDB:get_SDA_property]{soilDB::get_SDA_property()}}.}

\item{method}{Aggregation method string accepted by
\code{\link[soilDB:get_SDA_property]{soilDB::get_SDA_property()}}. \strong{PROVISIONAL default:} \code{"Weighted Average"} — confirm with PI.}

\item{top_depth}{Top of the depth interval in cm. \strong{PROVISIONAL default:}
\code{0} — confirm with PI.}

\item{bottom_depth}{Bottom of the depth interval in cm. \strong{PROVISIONAL
default:} \code{25} — confirm with PI.}
}
\value{
A data frame of soil properties, one row per mukey (or per
mukey × component depending on \code{method}), row-bound across all chunks.
}
\description{
Wraps \code{\link[soilDB:get_SDA_property]{soilDB::get_SDA_property()}} to retrieve one or more soil properties
aggregated over a specified depth interval for a vector of map unit keys
(mukeys). The mukey vector is split into chunks of at most 5 000 keys per
request to stay within SDA's request-size ceiling (soilDB issue #228
documents failures above approximately 15 000 mukeys per call). Results
from all chunks are row-bound into a single data frame.
}
\details{
\strong{Provisional defaults — confirm with PI before using in analysis:}
The \code{method}, \code{top_depth}, and \code{bottom_depth} defaults (\code{"Weighted Average"}, \code{0}–\code{25} cm) are starting points only. The depth interval and
aggregation method are scientific choices with direct consequences for
which soil horizon is represented; they must be confirmed by the PI before
results are used in any downstream analysis.
}
