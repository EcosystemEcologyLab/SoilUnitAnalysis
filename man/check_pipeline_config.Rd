% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_config.R
\name{check_pipeline_config}
\alias{check_pipeline_config}
\title{Check pipeline configuration}
\usage{
check_pipeline_config(
  required_env_vars = c("NEON_TOKEN"),
  required_dirs = c("data/snapshots", "data/overrides", "data/raw", "data/extracted",
    "data/processed", "outputs", "figures"),
  min_r_version = "4.4.0"
)
}
\arguments{
\item{required_env_vars}{Character vector of environment variable names
that must be set for the pipeline to run. Defaults to \code{"NEON_TOKEN"}
per \code{.env.example}.}

\item{required_dirs}{Character vector of project-relative directories that
must exist. Defaults to the directories declared in
\code{SCIENCE_PRINCIPLES_PIPELINES.md} "Data directory conventions".}

\item{min_r_version}{Minimum acceptable R version as a \code{numeric_version}.}
}
\value{
Invisibly, a named list with elements \code{r_version}, \code{project_root},
\code{env_vars} (named logical: TRUE if set and non-empty), and \code{dirs}
(named logical: TRUE if directory exists).
}
\description{
Verifies that the runtime environment satisfies the requirements declared
in \code{CLAUDE.md} and \code{SCIENCE_PRINCIPLES_PIPELINES.md}. Every numbered script
in \verb{scripts/} must call this function before doing any work.
}
\details{
Checks performed:
\itemize{
\item R version meets the minimum declared in \code{min_r_version}.
\item The working directory looks like the project root (contains \code{CLAUDE.md}
and a \code{DESCRIPTION} file).
\item Required project directories exist.
\item Required environment variables are set; missing ones emit a \code{warning()}
(not silently defaulted), per the "Fail loudly" rule in
\code{SCIENCE_PRINCIPLES.md}.
}

The function fails fast with \code{stop()} only on unrecoverable problems
(wrong R version, wrong working directory). All other issues are surfaced
as \code{warning()} so the user sees them but the pipeline can decide whether
to proceed.
}
