% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gssurgo_helpers.R
\name{read_gssurgo_raster}
\alias{read_gssurgo_raster}
\title{Read the MapunitRaster_10m layer from a gSSURGO FileGeodatabase}
\usage{
read_gssurgo_raster(gdb_path, aoi)
}
\arguments{
\item{gdb_path}{Path to the \code{gSSURGO_AZ.gdb} directory.}

\item{aoi}{An \code{sf} or \code{sfc} object defining the clip extent, already in
EPSG:5070. Fails loudly if \code{aoi} is not \code{sf}/\code{sfc} or if the clipped
raster contains zero non-NA cells.}
}
\value{
A \link[terra:SpatRaster]{terra::SpatRaster} of mukey values cropped and masked to \code{aoi}.
}
\description{
Reads the MapunitRaster_10m raster from the FileGeodatabase at \code{gdb_path}
using \code{\link[terra:rast]{terra::rast()}}, then crops and masks it to \code{aoi}.
}
\details{
\strong{The caller is responsible for reprojecting \code{aoi} to the gSSURGO native
CRS (EPSG:5070, CONUS Albers Equal Area) before passing it in.} This
function does not reproject.

GDAL's OpenFileGDB driver (GDAL >= 3.6) is required to read rasters from
the FileGDB format. The function calls \code{\link[terra:describe]{terra::describe()}} to enumerate
subdatasets and selects the one named \code{MapunitRaster_10m}.
}
