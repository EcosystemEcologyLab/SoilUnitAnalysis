% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soilweb_helpers.R
\name{query_ssurgo_polygons}
\alias{query_ssurgo_polygons}
\title{Query SSURGO map unit polygons from SDA}
\usage{
query_ssurgo_polygons(aoi, db = "SSURGO")
}
\arguments{
\item{aoi}{An \code{sf} or \code{sfc} object defining the area of interest. Both
types are accepted; \code{sfc} is the natural output of \code{\link[sf:st_as_sfc]{sf::st_as_sfc()}} on
a bounding box. The object must have a defined CRS; any projection is
accepted and will be transformed internally. Fails loudly if \code{aoi} is
neither \code{sf} nor \code{sfc}, or if the query returns zero features.}

\item{db}{SDA database to query. Passed to \code{\link[soilDB:SDA_spatialQuery]{soilDB::SDA_spatialQuery()}}.
Defaults to \code{"SSURGO"}.}
}
\value{
An \code{sf} polygon object of SSURGO map unit boundaries intersecting
the AOI, with \code{query_datetime_utc} and \code{aoi_wkt} attached as attributes.
}
\description{
Wraps \code{\link[soilDB:SDA_spatialQuery]{soilDB::SDA_spatialQuery()}} to retrieve SSURGO map unit polygons
that intersect a supplied area of interest. Polygons are clipped
server-side (\code{geomIntersection = TRUE}) to keep the download payload
small. The AOI is reprojected to EPSG:4326 (WGS 84) before the query
because SDA expects geographic coordinates.
}
\details{
Two provenance attributes are attached to the returned object:
\itemize{
\item \code{query_datetime_utc}: ISO 8601 UTC timestamp of the query.
\item \code{aoi_wkt}: WKT representation of the (unioned, WGS 84) AOI geometry.
}
}
