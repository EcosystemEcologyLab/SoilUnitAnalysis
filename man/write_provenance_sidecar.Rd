% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssurgo_helpers.R
\name{write_provenance_sidecar}
\alias{write_provenance_sidecar}
\title{Write a provenance sidecar YAML file}
\usage{
write_provenance_sidecar(output_path, input_sources, notes)
}
\arguments{
\item{output_path}{Path to the primary output file. The sidecar is written
to \code{paste0(output_path, ".provenance.yaml")}.}

\item{input_sources}{A named list of input source descriptors (URLs, DOIs,
or file paths). Passed through to the YAML unchanged.}

\item{notes}{Free-text string documenting manual decisions, overrides, or
deviations from defaults. An empty string is acceptable; the field must
be present.}
}
\value{
The sidecar path, invisibly.
}
\description{
Records pipeline provenance metadata alongside an output file, per the
requirements in \code{SCIENCE_PRINCIPLES_PIPELINES.md}. The sidecar is written
to \code{paste0(output_path, ".provenance.yaml")}.
}
\details{
Required metadata fields written:
\itemize{
\item \code{output_file}: basename of \code{output_path}.
\item \code{run_datetime_utc}: ISO 8601 UTC timestamp.
\item \code{pipeline_version}: short git commit hash, or \code{"unknown"} if git is
unavailable or the working directory is not a git repository.
\item \code{input_sources}: the supplied list, passed through unchanged.
\item \code{r_session_info}: output of \code{\link[utils:sessionInfo]{utils::sessionInfo()}}.
\item \code{notes}: the supplied string (required even if empty).
}
}
