% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gssurgo_helpers.R
\name{read_gssurgo_table}
\alias{read_gssurgo_table}
\title{Read a tabular layer from a gSSURGO FileGeodatabase}
\usage{
read_gssurgo_table(gdb_path, table_name, key_values, key_col = "mukey")
}
\arguments{
\item{gdb_path}{Path to the \code{gSSURGO_AZ.gdb} directory.}

\item{table_name}{Name of the GDB table or feature class to read (e.g.
\code{"Valu1"}, \code{"component"}, \code{"chorizon"}).}

\item{key_values}{Character or integer vector of key values to keep. Rows
where \code{key_col} is not in \code{key_values} are dropped.}

\item{key_col}{Name of the column in \code{table_name} used for filtering.
Defaults to \code{"mukey"} (correct for \code{Valu1} and \code{component}). Use
\code{"cokey"} when reading \code{chorizon}.}
}
\value{
A data frame of rows matching \code{key_values}.
}
\description{
Reads a non-spatial tabular layer from the FileGeodatabase at \code{gdb_path}
using \code{\link[sf:st_read]{sf::st_read()}} with \code{layer = table_name} (sf reads non-spatial
tables from a GDB this way). Filters rows where the column named \code{key_col}
is in \code{key_values}. Returns a plain data frame.
}
\details{
\strong{Join responsibility:} This function handles direct key filtering only.
The gSSURGO schema relationships are:
\itemize{
\item \code{Valu1.mukey} links directly to \code{mapunit.mukey} — use \code{key_col = "mukey"}.
\item \code{component.mukey} links to \code{mapunit.mukey} — use \code{key_col = "mukey"}.
\item \code{chorizon.cokey} links to \code{component.cokey}; there is no \code{mukey} column
in \code{chorizon} — use \code{key_col = "cokey"} and supply component cokeys.
}

The two-step join (\verb{mukey → cokey → chkey}) must be performed explicitly
in the calling script so the join logic is visible in the pipeline.
}
