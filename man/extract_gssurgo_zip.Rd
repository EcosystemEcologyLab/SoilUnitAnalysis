% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gssurgo_helpers.R
\name{extract_gssurgo_zip}
\alias{extract_gssurgo_zip}
\title{Extract the gSSURGO ZIP into a destination directory}
\usage{
extract_gssurgo_zip(zip_path, dest_dir)
}
\arguments{
\item{zip_path}{Path to the gSSURGO ZIP file.}

\item{dest_dir}{Directory into which the ZIP is extracted. Created if it
does not exist.}
}
\value{
Path to the extracted \code{gSSURGO_AZ.gdb} directory, invisibly.
}
\description{
Extracts \code{zip_path} into \code{dest_dir} using \code{\link[utils:unzip]{utils::unzip()}}. Idempotent:
if \code{dest_dir/gSSURGO_AZ.gdb} already exists the extraction is skipped and
the path is returned immediately. After extraction the function validates
that the expected GDB directory is present and fails loudly if it is not.
}
